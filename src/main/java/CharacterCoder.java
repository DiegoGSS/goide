public class CharacterCoder {

    //Depois implementamos a função e fazemos o teste falhar
    public char characterRotator(char inputChar){
        char rotated =  (char) (inputChar + 3);
        return rotated;
    }
}
